---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gpunodepools.scheduler.ishanchopra.dev
spec:
  group: scheduler.ishanchopra.dev
  names:
    kind: GPUNodePool
    listKind: GPUNodePoolList
    plural: gpunodepools
    singular: gpunodepool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GPUNodePool is the Schema for the gpunodepools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GPUNodePool
            properties:
              deviceType:
                default: gpu
                description: DeviceType is an optional human-readable accelerator
                  class.
                minLength: 1
                type: string
              devicesPerNode:
                description: DevicesPerNode is the number of accelerators per node.
                format: int32
                minimum: 1
                type: integer
              memoryMiBPerDevice:
                description: MemoryMiBPerDevice is the capacity per device in MiB.
                format: int32
                minimum: 1
                type: integer
              nodeCount:
                description: NodeCount is the number of logical nodes in the pool.
                format: int32
                minimum: 1
                type: integer
              profile:
                description: Profile is the hardware profile key used for this homogeneous
                  pool.
                minLength: 1
                type: string
            required:
            - devicesPerNode
            - memoryMiBPerDevice
            - nodeCount
            - profile
            type: object
          status:
            description: status defines the observed state of GPUNodePool
            properties:
              allocatedDevices:
                description: AllocatedDevices is the number of currently allocated
                  devices.
                format: int32
                type: integer
              availableDevices:
                description: AvailableDevices is the number of currently free devices.
                format: int32
                type: integer
              conditions:
                description: |-
                  conditions represent the current state of the GPUNodePool resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              totalDevices:
                description: TotalDevices is the total number of devices currently
                  registered in the simulator fleet.
                format: int32
                type: integer
              utilizationPct:
                description: UtilizationPct is the allocated device ratio as a percentage
                  in [0,100].
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
