# Worker Deployment (scale-focused): Deployment; KEDA ScaledObject is available but not enabled by default
# in this setup so that workers run at a fixed replica count and the scheduler bottleneck can be observed.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: system
resources:
  - deployment.yaml
  - rbac.yaml
