apiVersion: v1
kind: Service
metadata:
  name: simulator
  namespace: system
  labels:
    app.kubernetes.io/name: gpu-scheduler-simulator
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: gpu-scheduler-simulator
